import { EspecialidadPipe } from './especialidad.pipe';

describe('EspecialidadPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadPipe();
    expect(pipe).toBeTruthy();
  });
});
