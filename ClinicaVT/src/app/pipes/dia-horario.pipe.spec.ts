import { DiaHorarioPipe } from './dia-horario.pipe';

describe('DiaHorarioPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaHorarioPipe();
    expect(pipe).toBeTruthy();
  });
});
