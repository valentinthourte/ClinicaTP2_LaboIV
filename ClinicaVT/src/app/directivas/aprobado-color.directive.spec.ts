import { AprobadoColorDirective } from './aprobado-color.directive';

describe('AprobadoColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AprobadoColorDirective();
    expect(directive).toBeTruthy();
  });
});
